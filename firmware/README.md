# Firmware de integración

Este directorio contiene el juego y los programas que integran varios periféricos. Los drivers propios de cada módulo se mantienen en `cores/<periferico>/firmware/`.

El subdirectorio `blink_test/` muestra cómo usar el driver del periférico de ejemplo.
